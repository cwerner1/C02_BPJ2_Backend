language: java
jdk:
  - openjdk8

#branches:
#  only:
#    - master

# Travis CI enable caching
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

# clean Build
script: "./gradlew clean build --scan -s"

after_success:
  - ./gradlew jacocoTestReport coveralls